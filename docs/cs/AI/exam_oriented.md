## Chap 3


1. 人工智能开发中的特征工程是指自动选择最适合的特征进行模型训练的过程。 

??? tip "答案"
    - 错误

    - 特征工程是指从原始数据中提取、转换、选择和创建特征，以提高机器学习模型性能的过程。

---




2. 在训练深度学习模型时，增加模型的复杂性总是能提高其在测试数据上的表现。 

??? tip "答案"
    - 错误

    - **过拟合** (Overfitting)：当模型的复杂性增加时，它可能会在训练数据上表现得非常好，但**在测试数据上表现不佳**。这种现象称为过拟合。过拟合的模型在训练数据上**学到了太多的细节和噪声**，导致它**无法很好地泛化到新的、未见过的数据**。

## Chap 4


## Chap 5

|名词|描述|
|---|----|
|支持向量机（SVM）|监督学习：寻找**最佳的划分两类点的一条直线**（超平面），同时最大化类别之间的间隔|
|交叉验证|评估并优化模型：通过将数据集划分为多个子集，反复训练和验证模型，以获得更稳定和可靠的性能评估结果。有助于**避免模型过拟合和欠拟合**|
|梯度下降|优化模型：为了**最小化损失函数**，通过沿损失函数的梯度方向不停迭代变化，从而靠近“谷底”|
|决策树|监督学习：选择最佳特征来构建一棵决策树，使得决策自动且合理，用于分类和回归|
|K-means|无监督学习：让**无标注**的数据自行分类出各个cluster，让点有归属，每次分类点就让它去最近的cluster，并且及时更新cluster centroid|
|KNN|监督学习：帮助样本点分类，不过只关注样本点**周围已知样本点**的情况|
|逻辑回归|监督学习：实际上是一种分类算法（二分类），利用逻辑函数（sigmoid）将线性回归的输出映射到(0,1)区间|




1. 在k折交叉验证中，数据集被分成k个不同的子集，每次使用一个子集作为验证集，其余子集作为训练集。 (判断题) 

??? tip "答案"
    - 正确

    - 在 k 折交叉验证中，数据集被分成 k 个不同的子集（或称为折）。每次使用一个子集作为验证集，其余 k-1 个子集作为训练集。这个过程重复 k 次，每次选择不同的子集作为验证集。最终，模型的性能通过所有 k 次验证的结果进行评估。

2. 在防止过拟合的技术中，下列哪种方法通过在模型中引入惩罚项来减少过拟合？ 

A . 交叉验证

B . 正则化

C . 数据扩充

D . 提升法 为什么

??? tip "答案"
    - B. 正则化

    - **正则化** 是一种通过在损失函数中添加惩罚项来减少模型复杂性的方法，从而防止过拟合。常见的正则化方法包括 L1 正则化（Lasso）和 L2 正则化（Ridge）。这些方法通过在损失函数中添加参数的绝对值或平方和的惩罚项，迫使模型参数变小，从而减少模型的复杂性和过拟合的风险。

    - 交叉验证是一种评估模型性能的方法，通过将数据集划分为多个子集，反复训练和验证模型，以获得更稳定的性能评估结果。虽然交叉验证有助于选择模型和参数，但它本身并不通过引入惩罚项来减少过拟合。

    - 数据扩充是一种通过生成更多训练数据来防止过拟合的方法，特别是在图像处理领域。它通过对现有数据进行变换（如旋转、缩放、翻转等）来生成新的数据样本，从而增加数据集的多样性。但它并不涉及在模型中引入惩罚项。

    - 提升法（Boosting）是一种集成学习方法，通过组合多个弱分类器来提高模型的性能。虽然提升法可以减少过拟合，但它的主要机制是通过加权组合多个模型，而不是通过在模型中引入惩罚项。


3.  K-means 聚类算法适合处理包含噪声数据的聚类任务。（判断题）

??? tip "答案"
    - 错误

    - K-means 聚类算法并不适合处理包含噪声数据的聚类任务。以下是原因：

    - 敏感性：K-means 对噪声和异常值非常敏感。噪声点和异常值会**显著影响聚类中心的计算**，导致聚类结果不准确。
    
    - 硬分配：K-means 是一种硬分配算法，每个数据点必须分配到一个簇中，即使是噪声点也不例外。这会导致噪声点对聚类中心的计算产生不良影响。


4. 使用 K-means 聚类时，必须事先定义 K 值，且一旦确定后不能更改。（判断题）

??? tip "答案"
    - 错误

    - 虽然 K-means 聚类算法确实需要事先定义 K 值（即聚类的数量），但这并不意味着 K 值一旦确定后就不能更改。以下是原因：

    - 多次实验：在实际应用中，通常会通过**多次实验和评估不同的 K 值来找到最合适的聚类数量**。可以使用肘部法（Elbow Method）、轮廓系数（Silhouette Coefficient）等方法来评估不同 K 值的聚类效果。

    - 动态调整：在某些改进的 K-means 算法中，可以动态调整 K 值。例如，X-means 是一种扩展的 K-means 算法，它可以自动选择最优的 K 值。

## Chap 8

1. 在人工神经网络中，隐藏层的主要作用是：
单选题
(5 分)

    A. 直接输出分类结果

    B. 存储输入和输出数据

    C. 提取和学习数据的特征

    D. 优化梯度下降的速度

2. 反向传播算法在神经网络中用于：
单选题
(5 分)

    A. 更新权重和偏置

    B. 增加神经元数量

    C. 初始化网络参数

    D. 增强网络的非线性特性

3. 卷积神经网络（CNN）中，卷积层的主要作用是：
单选题
(5 分)

    A. 减少数据的维度

    B. 提取数据的局部特征

    C. 增加模型复杂度

    D. 合并不同特征

4. 在 CNN 中，池化层的作用是：
单选题
(5 分)

    A. 减少计算复杂度

    B. 提高特征的分辨率

    C. 保留图像的所有信息

    D. 增加网络的非线性

5. 在卷积神经网络中，多个卷积核的存在可以：
单选题
(5 分)

    A. 提取不同类型的特征

    B. 降低网络的计算复杂度

    C. 增强特征图的分辨率

    D. 限制网络的输出范围

6. 在卷积神经网络中，全连接层的主要功能是：
单选题
(5 分)

    A. 提取特征

    B. 进行非线性变换

    C. 对特征进行整合，生成最终的输出

    D. 降低特征的维度

7. 卷积层是卷积神经网络的核心组件，用于提取图像的局部特征。
判断题
(5 分)

    A. 正确

    B. 错误

8. 在 CNN 中，池化层能够提高特征的分辨率，从而保留更多细节。
判断题
(5 分)

    A. 正确

    B. 错误

9. 全连接层的参数数量通常比卷积层少，因此计算成本较低。
判断题
(5 分)

    A. 正确

    B. 错误

10. 深度神经网络中的层数越多，模型的表达能力就越强，但可能会增加梯度消失的风险。
判断题
(5 分)

    A. 正确

    B. 错误

11. 卷积神经网络中，深度越大意味着需要更多的数据才能避免过拟合。
判断题
(5 分)

    A. 正确

    B. 错误

12. 反向传播算法会从输出层开始，逐层计算梯度并更新权重。
判断题
(5 分)

    A. 正确

    B. 错误

13. 在训练深层神经网络时，梯度消失是常见的问题之一，尤其在 Sigmoid 激活函数中。
判断题
(5 分)

    A. 正确

    B. 错误

14. 卷积核的大小、数量和步幅的设置会直接影响特征图的输出尺寸和信息提取能力。
判断题
(5 分)

    A. 正确

    B. 错误

15. 在 CNN 中，每次卷积操作都会增大输入特征图的尺寸。
判断题
(5 分)

    A. 正确

    B. 错误

16. CNN 的卷积层可以同时处理不同的输入通道，例如 RGB 图像的三个通道。
判断题
(5 分)

    A. 正确

    B. 错误

17. 神经网络中的学习率决定了：
单选题
(5 分)

    A. 模型的深度

    B. 模型的复杂度

    C. 每次权重更新的步长

    D. 激活函数的类型

18. 在训练神经网络时，如果学习率过大，可能导致：
单选题
(5 分)

    A. 模型在训练集上表现很好

    B. 损失函数震荡或无法收敛

    C. 训练时间较长

    D. 模型欠拟合

19. 以下哪种方法可以有效缓解神经网络的过拟合问题？
单选题
(5 分)

    A. 增加训练数据

    B. 增大模型深度

    C. 减小正则化参数

    D. 增大学习率

20. 在卷积神经网络中，Dropout 是一种：
单选题
(5 分)

    A. 激活函数

    B. 数据增强方法

    C. 正则化技术

    D. 权重初始化方法

交卷

## Chap 9

1. 循环神经网络（RNN） 的主要特点是：
单选题
(5 分)

    A. 数据通过多个完全独立的层处理

    B. 能处理序列数据，具有时间记忆能力

    C. 只能用于静态数据分析

    D. 适用于图像数据的特征提取

2. LSTM 网络中的输入门的作用是：
单选题
(5 分)

A. 决定当前输入信息对长期记忆的影响

B. 决定当前输入信息是否参与输出

C. 控制隐藏状态的更新

D. 控制梯度流向

3. 在处理时间序列数据时，以下哪种方法可以缓解梯度爆炸问题？
单选题
(5 分)

A. 使用激活函数

B. 减少隐藏层数量

C. 使用梯度裁剪

D. 增加学习率

4. 编码器-解码器模型的主要应用场景是：
单选题
(5 分)

A. 图像分类

B. 机器翻译

C. 时间序列预测

D. 特征提取

5. 以下哪种任务适合使用 RNN？
单选题
(5 分)

A. 图像分类

B. 文本生成

C. 图像分割

D. 时间不相关的静态任务

6. 在 LSTM 的门控机制中，遗忘门（Forget Gate）的作用是：
单选题
(5 分)

A. 控制哪些信息需要被保留

B. 控制输出的概率分布

C. 负责初始化隐藏状态

D. 控制哪些信息需要被丢弃

7. 在多分类任务中，输出层的激活函数通常是：
单选题
(5 分)

A. Sigmoid

B. ReLU

C. Softmax

D. Tanh

8. LSTM 网络的主要优点是：
单选题
(5 分)

A. 能有效缓解梯度消失问题

B. 具有更少的参数，计算速度更快

C. 更适合处理静态数据

D. 不需要反向传播算法

9. 在处理长序列数据时，普通 RNN 容易出现以下哪种问题？
单选题
(5 分)

A. 参数过多

B. 过拟合

C. 梯度消失或梯度爆炸

D. 输入序列信息丢失

10. 以下哪种结构可以更好地处理长序列数据中的长依赖问题？
单选题
(5 分)

A. 普通 RNN

B. 单向 RNN

C. 双向 RNN

D. LSTM 或 GRU

11. LSTM 是一种特殊的 RNN，它通过门控机制解决了梯度消失问题。
判断题
(5 分)

A. 正确

B. 错误

12. RNN 只能用于有监督学习，无法应用于无监督学习。
判断题
(5 分)

A. 正确

B. 错误

13. 在自然语言处理中，RNN 常用于文本分类和序列生成等任务。
判断题
(5 分)

A. 正确

B. 错误

14. 在时间序列预测中，LSTM 比传统 RNN 表现更优，因为它更擅长处理长依赖关系。
判断题
(5 分)

A. 正确

B. 错误

15. 使用更大的批量大小可以完全解决梯度消失问题。
判断题
(5 分)

A. 正确

B. 错误

16. 编码器-解码器结构的注意力机制可以动态选择输入序列中最相关的信息。
判断题
(5 分)

A. 正确

B. 错误

17. 注意力机制只适用于 RNN，不适用于其他深度学习模型。
判断题
(5 分)

A. 正确

B. 错误

18. 使用 ReLU 激活函数可以完全解决梯度消失问题。
判断题
(5 分)

A. 正确

B. 错误

19. 注意力机制能够动态选择输入序列中与当前任务相关的重要部分。
判断题
(5 分)

A. 正确

B. 错误

20. 在序列到序列任务中，编码器负责生成输入序列的隐藏表示。
判断题
(5 分)

A. 正确

B. 错误

交卷

---

# Chap 12

1. 在自然语言处理中，“Token”的含义是：
单选题
(5 分)

    A. 一个完整的句子

    B. 一个独立的词或字符

    C. 一个预训练模型

    D. 一个词嵌入算法

2. 在文本相似度计算中，以下哪种方法基于两点之间的直线距离？
单选题
(5 分)

    A. 曼哈顿距离

    B. 欧氏距离

    C. 余弦相似度

    D. Jaccard相似度

3. 以下哪一个不是预训练大模型的代表？
单选题
(5 分)

    A. BERT

    B. GPT

    C. T5

    D. LDA

4. 分词技术的主要目的是：
单选题
(5 分)

    A. 减少文本长度

    B. 提高存储效率

    C. 将文本分割为最小处理单位

    D. 删除无意义的词

5. 关于文本生成技术，下列描述错误的是：
单选题
(5 分)

    A. 它可以生成自然流畅的句子

    B. 通过从知识库中抽取答案实现

    C. 基于自回归技术逐词生成

    D. 广泛应用于聊天机器人中

6. 以下哪种模型可以处理不同长度的输入和输出序列？
单选题
(5 分)

    A. 词袋模型（BoW）

    B. 独热编码

    C. Seq2Seq

    D. TF-IDF

7. 关于 Token 的描述，下列哪项是正确的？
单选题
(5 分)

    A. Token 总是一个完整的单词

    B. Token 的长度是固定的

    C. Token 可以是字符、字母或子词

    D. Token 是特定模型的训练参数

8. 在分词过程中，中文分词的一个关键挑战是：
单选题
(5 分)

    A. 缺乏标准的分隔符

    B. 文本长度过长

    C. 模型无法理解语法规则

    D. 英文词汇对中文的影响

9. 以下哪种方法不属于文本相似度的计算方式？
单选题
(5 分)

    A. 欧氏距离

    B. 曼哈顿距离

    C. Jaccard 相似度

    D. 模型参数优化

10. 关于词向量的分布式表示，下列哪种描述是错误的？
单选题
(5 分)

    A. 每个词被表示为一个稠密的向量

    B. 它能够捕获单词之间的语义关系

    C. 它与独热编码表示相同

    D. 它可以用来表示一个句子的语义

11. 词向量可以通过分布式表示法和独热表示法两种方式实现。
判断题
(5 分)

    A. 正确

    B. 错误

12. 在 Seq2Seq 模型中，编码器的任务是生成目标序列。
判断题
(5 分)

    A. 正确

    B. 错误

13. Jaccard 相似度用于衡量两个集合间的交集与并集的比例。
判断题
(5 分)

    A. 正确

    B. 错误

14. Word2Vec 模型中，维度越高的词向量表示的信息越丰富。
判断题
(5 分)

    A. 正确

    B. 错误

15. Word2Vec 模型需要大量的标注数据进行训练。
判断题
(5 分)

    A. 正确

    B. 错误

16. 统计语言模型只能解决简单的文本预测问题。
判断题
(5 分)

    A.正确

    B.错误

17. Seq2Seq模型的编码器和解码器可以使用不同类型的神经网络。
判断题
(5 分)

    A.正确

    B.错误

18. Word2Vec模型中的CBOW算法是通过给定的上下文词预测中心词。
判断题
(5 分)

    A.正确


    B.错误

19. Seq2Seq模型无法处理不同长度的输入和输出。
判断题
(5 分)

    A.正确


    B.错误

20. BERT的全名是Bidirectional Encoder Representations from Transformers。
判断题
(5 分)

    A.正确


    B.错误


交卷